#include<stdio.h>
int main()
{
/*
        int i,a[5]={};
        for(i=0;i<5;i++)
        {
                printf("a[%d] = %d\nsize = %d\n",i,a[i],sizeof(a[i]));
        }
*/
/*
        int i,a[10]={2.4,5.6,7.3};
        for(i=0;i<10;i++)
        {
                printf("\na[%d] = %d\n\tsize = %d\n",i,a[i],sizeof(a[i]));
        }
*/

/*
        int i,a[3]={1,2,3,4,5};
        for(i=0;i<10;i++)
        {
                printf("\na[%d] = %d\n\tsize = %d\n",i,a[i],sizeof(a[i]));
        }
*/
     
	
  
/*
        int i,a[0]={};
        for(i=0;i<10;i++)
        {
                printf("\na[%d] = %d\n\tsize = %d\n",i,a[i],sizeof(a[i]));
        }
*/
	
/*
        int i,a[0]={1,2,3,4,5};
        for(i=0;i<10;i++)
        {
                printf("\na[%d] = %d\n\tsize = %d\n",i,a[i],sizeof(a[i]));
        }
*/
/*
        int i,a[]={};
        for(i=0;i<10;i++)
        {
                printf("\na[%d] = %d\n\tsize = %d\n",i,a[i],sizeof(a[i]));
        }
*/
///*
        int i,a['a'];
        for(i=0;i<10;i++)
        {
                printf("\na[%d] = %d\n\tsize = %d\n",i,a[i],sizeof(a[i]));
        }
//*/

  

	return 0;
}

